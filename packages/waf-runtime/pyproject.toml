# packages/waf-runtime/pyproject.toml
[project]
name = "waf-runtime"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "D7x7z49", email = "85430783+D7x7z49@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "waf-core",
]

[project.scripts]
waf-runtime = "waf_runtime:main"

[build-system]
requires = ["uv_build>=0.10.11,<0.11.0"]
build-backend = "uv_build"

[tool.uv.sources]
waf-core = { workspace = true }
